#include<stdio.h>
#include<math.h>
int main()
{
    // double x,y;
    // scanf("%lf %lf",&x,&y);
    // double answer=pow(x,y);
    // printf("%0.2lf",answer);
    int x,y;
     scanf("%d %d",&x,&y);
     int answer=pow(x,y);
     printf("%d",answer);
    return 0;
}